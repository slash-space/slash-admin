// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"slash-admin/app/bookstore/ent/bsuser"
	"slash-admin/app/bookstore/ent/predicate"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// BsUserQuery is the builder for querying BsUser entities.
type BsUserQuery struct {
	config
	limit      *int
	offset     *int
	unique     *bool
	order      []OrderFunc
	fields     []string
	predicates []predicate.BsUser
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the BsUserQuery builder.
func (buq *BsUserQuery) Where(ps ...predicate.BsUser) *BsUserQuery {
	buq.predicates = append(buq.predicates, ps...)
	return buq
}

// Limit adds a limit step to the query.
func (buq *BsUserQuery) Limit(limit int) *BsUserQuery {
	buq.limit = &limit
	return buq
}

// Offset adds an offset step to the query.
func (buq *BsUserQuery) Offset(offset int) *BsUserQuery {
	buq.offset = &offset
	return buq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (buq *BsUserQuery) Unique(unique bool) *BsUserQuery {
	buq.unique = &unique
	return buq
}

// Order adds an order step to the query.
func (buq *BsUserQuery) Order(o ...OrderFunc) *BsUserQuery {
	buq.order = append(buq.order, o...)
	return buq
}

// First returns the first BsUser entity from the query.
// Returns a *NotFoundError when no BsUser was found.
func (buq *BsUserQuery) First(ctx context.Context) (*BsUser, error) {
	nodes, err := buq.Limit(1).All(ctx)
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{bsuser.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (buq *BsUserQuery) FirstX(ctx context.Context) *BsUser {
	node, err := buq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first BsUser ID from the query.
// Returns a *NotFoundError when no BsUser ID was found.
func (buq *BsUserQuery) FirstID(ctx context.Context) (id uint64, err error) {
	var ids []uint64
	if ids, err = buq.Limit(1).IDs(ctx); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{bsuser.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (buq *BsUserQuery) FirstIDX(ctx context.Context) uint64 {
	id, err := buq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single BsUser entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one BsUser entity is found.
// Returns a *NotFoundError when no BsUser entities are found.
func (buq *BsUserQuery) Only(ctx context.Context) (*BsUser, error) {
	nodes, err := buq.Limit(2).All(ctx)
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{bsuser.Label}
	default:
		return nil, &NotSingularError{bsuser.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (buq *BsUserQuery) OnlyX(ctx context.Context) *BsUser {
	node, err := buq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only BsUser ID in the query.
// Returns a *NotSingularError when more than one BsUser ID is found.
// Returns a *NotFoundError when no entities are found.
func (buq *BsUserQuery) OnlyID(ctx context.Context) (id uint64, err error) {
	var ids []uint64
	if ids, err = buq.Limit(2).IDs(ctx); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{bsuser.Label}
	default:
		err = &NotSingularError{bsuser.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (buq *BsUserQuery) OnlyIDX(ctx context.Context) uint64 {
	id, err := buq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of BsUsers.
func (buq *BsUserQuery) All(ctx context.Context) ([]*BsUser, error) {
	if err := buq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	return buq.sqlAll(ctx)
}

// AllX is like All, but panics if an error occurs.
func (buq *BsUserQuery) AllX(ctx context.Context) []*BsUser {
	nodes, err := buq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of BsUser IDs.
func (buq *BsUserQuery) IDs(ctx context.Context) ([]uint64, error) {
	var ids []uint64
	if err := buq.Select(bsuser.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (buq *BsUserQuery) IDsX(ctx context.Context) []uint64 {
	ids, err := buq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (buq *BsUserQuery) Count(ctx context.Context) (int, error) {
	if err := buq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return buq.sqlCount(ctx)
}

// CountX is like Count, but panics if an error occurs.
func (buq *BsUserQuery) CountX(ctx context.Context) int {
	count, err := buq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (buq *BsUserQuery) Exist(ctx context.Context) (bool, error) {
	if err := buq.prepareQuery(ctx); err != nil {
		return false, err
	}
	return buq.sqlExist(ctx)
}

// ExistX is like Exist, but panics if an error occurs.
func (buq *BsUserQuery) ExistX(ctx context.Context) bool {
	exist, err := buq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the BsUserQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (buq *BsUserQuery) Clone() *BsUserQuery {
	if buq == nil {
		return nil
	}
	return &BsUserQuery{
		config:     buq.config,
		limit:      buq.limit,
		offset:     buq.offset,
		order:      append([]OrderFunc{}, buq.order...),
		predicates: append([]predicate.BsUser{}, buq.predicates...),
		// clone intermediate query.
		sql:    buq.sql.Clone(),
		path:   buq.path,
		unique: buq.unique,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Username string `json:"username,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.BsUser.Query().
//		GroupBy(bsuser.FieldUsername).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (buq *BsUserQuery) GroupBy(field string, fields ...string) *BsUserGroupBy {
	grbuild := &BsUserGroupBy{config: buq.config}
	grbuild.fields = append([]string{field}, fields...)
	grbuild.path = func(ctx context.Context) (prev *sql.Selector, err error) {
		if err := buq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		return buq.sqlQuery(ctx), nil
	}
	grbuild.label = bsuser.Label
	grbuild.flds, grbuild.scan = &grbuild.fields, grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Username string `json:"username,omitempty"`
//	}
//
//	client.BsUser.Query().
//		Select(bsuser.FieldUsername).
//		Scan(ctx, &v)
func (buq *BsUserQuery) Select(fields ...string) *BsUserSelect {
	buq.fields = append(buq.fields, fields...)
	selbuild := &BsUserSelect{BsUserQuery: buq}
	selbuild.label = bsuser.Label
	selbuild.flds, selbuild.scan = &buq.fields, selbuild.Scan
	return selbuild
}

func (buq *BsUserQuery) prepareQuery(ctx context.Context) error {
	for _, f := range buq.fields {
		if !bsuser.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if buq.path != nil {
		prev, err := buq.path(ctx)
		if err != nil {
			return err
		}
		buq.sql = prev
	}
	return nil
}

func (buq *BsUserQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*BsUser, error) {
	var (
		nodes = []*BsUser{}
		_spec = buq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*BsUser).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &BsUser{config: buq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	if len(buq.modifiers) > 0 {
		_spec.Modifiers = buq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, buq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (buq *BsUserQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := buq.querySpec()
	if len(buq.modifiers) > 0 {
		_spec.Modifiers = buq.modifiers
	}
	_spec.Node.Columns = buq.fields
	if len(buq.fields) > 0 {
		_spec.Unique = buq.unique != nil && *buq.unique
	}
	return sqlgraph.CountNodes(ctx, buq.driver, _spec)
}

func (buq *BsUserQuery) sqlExist(ctx context.Context) (bool, error) {
	switch _, err := buq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

func (buq *BsUserQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   bsuser.Table,
			Columns: bsuser.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeUint64,
				Column: bsuser.FieldID,
			},
		},
		From:   buq.sql,
		Unique: true,
	}
	if unique := buq.unique; unique != nil {
		_spec.Unique = *unique
	}
	if fields := buq.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, bsuser.FieldID)
		for i := range fields {
			if fields[i] != bsuser.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := buq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := buq.limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := buq.offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := buq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (buq *BsUserQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(buq.driver.Dialect())
	t1 := builder.Table(bsuser.Table)
	columns := buq.fields
	if len(columns) == 0 {
		columns = bsuser.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if buq.sql != nil {
		selector = buq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if buq.unique != nil && *buq.unique {
		selector.Distinct()
	}
	for _, m := range buq.modifiers {
		m(selector)
	}
	for _, p := range buq.predicates {
		p(selector)
	}
	for _, p := range buq.order {
		p(selector)
	}
	if offset := buq.offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := buq.limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// Modify adds a query modifier for attaching custom logic to queries.
func (buq *BsUserQuery) Modify(modifiers ...func(s *sql.Selector)) *BsUserSelect {
	buq.modifiers = append(buq.modifiers, modifiers...)
	return buq.Select()
}

// BsUserGroupBy is the group-by builder for BsUser entities.
type BsUserGroupBy struct {
	config
	selector
	fields []string
	fns    []AggregateFunc
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Aggregate adds the given aggregation functions to the group-by query.
func (bugb *BsUserGroupBy) Aggregate(fns ...AggregateFunc) *BsUserGroupBy {
	bugb.fns = append(bugb.fns, fns...)
	return bugb
}

// Scan applies the group-by query and scans the result into the given value.
func (bugb *BsUserGroupBy) Scan(ctx context.Context, v any) error {
	query, err := bugb.path(ctx)
	if err != nil {
		return err
	}
	bugb.sql = query
	return bugb.sqlScan(ctx, v)
}

func (bugb *BsUserGroupBy) sqlScan(ctx context.Context, v any) error {
	for _, f := range bugb.fields {
		if !bsuser.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("invalid field %q for group-by", f)}
		}
	}
	selector := bugb.sqlQuery()
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := bugb.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

func (bugb *BsUserGroupBy) sqlQuery() *sql.Selector {
	selector := bugb.sql.Select()
	aggregation := make([]string, 0, len(bugb.fns))
	for _, fn := range bugb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	// If no columns were selected in a custom aggregation function, the default
	// selection is the fields used for "group-by", and the aggregation functions.
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(bugb.fields)+len(bugb.fns))
		for _, f := range bugb.fields {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	return selector.GroupBy(selector.Columns(bugb.fields...)...)
}

// BsUserSelect is the builder for selecting fields of BsUser entities.
type BsUserSelect struct {
	*BsUserQuery
	selector
	// intermediate query (i.e. traversal path).
	sql *sql.Selector
}

// Scan applies the selector query and scans the result into the given value.
func (bus *BsUserSelect) Scan(ctx context.Context, v any) error {
	if err := bus.prepareQuery(ctx); err != nil {
		return err
	}
	bus.sql = bus.BsUserQuery.sqlQuery(ctx)
	return bus.sqlScan(ctx, v)
}

func (bus *BsUserSelect) sqlScan(ctx context.Context, v any) error {
	rows := &sql.Rows{}
	query, args := bus.sql.Query()
	if err := bus.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (bus *BsUserSelect) Modify(modifiers ...func(s *sql.Selector)) *BsUserSelect {
	bus.modifiers = append(bus.modifiers, modifiers...)
	return bus
}
